<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.review">

	<select id="reviewListByHos" parameterType="String"
		resultType="hashmap">
	<![CDATA[
		select 	p.pname as name, 
				r.content as content,
				r.star as star, 
				r.birth as birth
		from review r, patient p 
		where r.comnum=#{comnum} AND r.pidnum=p.pidnum
	]]>
	</select>
	
	
	<select id="myReviewListByid" parameterType="String" resultType="hashmap">
	<![CDATA[
	SELECT h.hname as name
     	 , res.resdate as date
     	 , h.department as department
     	 , r.star as star
     	 , r.content as content 
	  FROM patient p
   	   	 , review r
   		 , hospital h
   		 , reservation res
 	 WHERE 1=1
	  AND r.pidnum = #{pidnum}
	  AND r.comnum = h.comnum
	  AND res.pidnum = #{pidnum}
	  AND h.comnum = res.comnum
	  ORDER BY res.resdate desc
	]]>
	</select>

</mapper>