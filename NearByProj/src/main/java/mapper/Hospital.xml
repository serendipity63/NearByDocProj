<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.hospital">
	<insert id="insertHospital" parameterType="Hospital"
		useGeneratedKeys="true" keyProperty="number">
    <![CDATA[
       insert into Hospital (date,time,name,phone,address,opinion,subject,complete) values 
       (#{date},#{time},#{name},#{phone},#{address},#{opinion},#{subject},#{complete}) 
    ]]>
	</insert>
	<update id="updateHospital" parameterType="Hospital">
		<trim prefix="SET" suffixOverrides=",">
			<if test="subject !=null">subject=#{subject},</if>
			<if test="email !=null">email=#{email},</if>
			<if test="number !=null">number=#{number},</if>
			<if test="address !=null">address=#{address},</if>
			<if test="clinictime !=null">clinic=#{clinic},</if>
			<if test="lunchtime !=null">lunch=#{lunch},</if>
		</trim>
		where num=#{num}
	</update>
	



</mapper>