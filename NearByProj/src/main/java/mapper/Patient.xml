<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.patient">
	<select id="selectpatient" parameterType="String" resultType="Patient">
		<![CDATA[
		select #{pidnum},#{pemail},#{date},#{time},#{pname},#{number},#{ptel},#{paddress} from patient where id=#{pidnum}
		]]>
	</select>
	
	<insert id="insertpatient" parameterType="Patient">
		<![CDATA[
			insert into Patient (pidnum,pemail,ppassword,date,time,pname,number,ptel,paddress) values (#{pidnum},#{pemail},#{ppassword},#{date},#{time},#{pname},#{number},#{ptel},#{paddress})
		]]>
	
	</insert>
</mapper>